# We use a couple Heroku buildpacks to get Elixir and Phoenix working.
# This config file isn't required, but checking it into the repo makes
# our deployed Elixir version explicit.

erlang_version=20.1
elixir_version=1.8.1
always_rebuild=false
runtime_path=/app
