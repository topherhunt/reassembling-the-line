defmodule Zb.EmailTest do
  use Zb.ModelCase, async: true

  test "stuff" do
    # TODO: Unit testing emails should be easy because they're just structs.
    # See https://hexdocs.pm/bamboo/readme.html#testing
  end
end
