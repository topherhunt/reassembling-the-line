<h1><%= @project.name %></h1>
<% owner = RTL.Accounts.get_user_by!(admin_on_project: @project) %>
<p><%= raw gettext("This project is run by <a href=\"mailto:%{owner_email}\">%{owner_name}</a> using the <a href=\"/\">RTL</a> platform.", owner_email: owner.email, owner_name: owner.name) %></p>
